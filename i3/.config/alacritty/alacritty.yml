import:
  - ~/.config/alacritty/xterm.yml

env:
  TERM: xterm
font:
  normal:
    family: Terminus
    style: Regular
  size: 23.0
key_bindings:
  - { key: Return, mods: Control, action: SpawnNewInstance }
commands:
  - { name: SpawnNewInstance, command: alacritty & disown }
